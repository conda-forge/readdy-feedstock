{% set name = "readdy" %}
{% set version = "2.0.14" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://github.com/{{ name }}/{{ name }}/archive/v{{ version }}.tar.gz
  sha256: 66e909f71a8f4e537085144155023797b0cc0a9abf40a075e28d9e58f49e3028
  patches:
    - fix-hdf5-version-check.patch

build:
  number: 0
  skip: true  # [win]
  rpaths:
    - lib/

requirements:
  build:
    - {{ compiler('c') }}
    - {{ stdlib("c") }}
    - {{ compiler('cxx') }}
    - cmake
    - ninja
  host:
    - python
    - blosc
    - h5py
    - hdf5
    - fmt
    - nlohmann_json
    - numpy
    - pybind11
    - pybind11-abi
    - spdlog
    - zlib
  run:
    - python
    - h5py
    - pint
    - tqdm
    - hdf5

test:
  imports:
    - readdy
  requires:
    - pytest
  commands:
    - python -m pytest --pyargs readdy.tests -v

about:
  home: https://github.com/readdy/readdy
  license: BSD-3-Clause
  license_family: BSD
  license_file: LICENSE
  summary: 'Python / C++ based particle reaction-diffusion simulator'
  description: |
    ReaDDy (standing for [Rea]ction [D]iffusion [Dy]namics) is a particle based
    reaction-diffusion simulator that can be configured and run with Python 3.6+.
    The Python interface wraps around a C++ implementation for good performance.
    Trajectories are written using the flexible HDF5 format.
  doc_url: https://{{ name }}.github.io/
  dev_url: https://github.com/{{ name }}/{{ name }}

extra:
  recipe-maintainers:
    - clonker
    - chrisfroe
